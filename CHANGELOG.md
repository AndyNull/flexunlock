# 更新日志

本项目采用语义化版本。所有可编译模块的 `versionName` 和 `versionCode` 均来自根目录 `gradle.properties`。

## 1.7.1 - 2026-08-24

### 修复

- 完整 DeX 最近任务的“仅标题”模式改为纵向滚动列表，避免标题项横向移出可视区域。
- 移除 Honeyboard 键盘尺寸强制 Hook，恢复系统原生键盘尺寸调整和二次调整。
- 完整 DeX 下普通应用即使没有原始 Cover intent 也由 system_server 放行到 display 1，避免“需要展开手机”。
- 普通外屏桌面不再加载完整 DeX 专用键盘尺寸逻辑或最近任务覆盖层。
- 修复普通应用抽屉完成打开后跳到左上角的问题，恢复 1.7.0 的原生布局 hook 范围。
- 修复底部中间 HOME 手势被错误转发到最近任务导致无法返回桌面的问题。

### 验证

- `testReleaseUnitTest`、`compileReleaseKotlin`、`lintVitalRelease` 和签名安装通过。
- Z Flip5 重启后 system_server Hook 正常加载；相册 display 1 启动日志确认 `allow=true`。

## 1.7.0 - 2026-08-23

### 新增

- 外屏控制增加 display 1 当前分辨率、DPI 查询，以及自定义参数应用和恢复。
- 增加完整外屏 DeX 模式开关，接入三星任务栏、应用菜单和最近任务路径。
- 应用管理增加全局显示比例；单应用全屏比例和弹窗尺寸继续保持更高优先级。
- 外屏应用列表与显示配置协议补充工作资料、多用户和全局默认配置支持。

### 修复

- 修复完整 DeX 模式切换时控制 Activity 被销毁，导致延迟 ENABLE 丢失、外屏停留在 SystemUI SubHome 的问题；重启事务改由 system_server 完成。
- 修复 One UI 8.5 锁屏访问状态判定，避免未解锁启动普通应用及锁屏/桌面所有权反复切换。
- 修复非 340 DPI 下 QS 亮度区域、应用缩放密度换算和外屏桌面图标布局异常。
- 修复完整 DeX 最近任务入口、任务栏状态与应用启动 display 路由。

### 优化

- 缩小外屏锁屏、Launcher 和 SystemUI 热路径中的重复查询与重复布局。
- 完整 DeX 与稳定外屏桌面使用独立布局策略，避免普通模式继承 DeX 网格参数。

## 1.6.9.1 - 2026-08-20

### 新增

- 应用显示设置增加全局默认入口，单应用配置继续保持更高优先级。

### 修复

- 优化应用筛选弹窗分组和反选状态，并修复全屏模式仍显示弹窗宽高字段。
- 增大带名称桌面图标的安全尺寸，恢复桌面与应用抽屉图标尺寸设置持久化。
- 修复壁纸预览因旧测量尺寸和二次缩放导致的裁切。
- 统一 QS 与通知页模糊宿主和顶部状态栏高度，减少初始下拉阶段的重复视觉写入。

### 优化

- 缓存折叠状态、顶层任务和 QS 磁贴几何，降低唤醒与状态栏展开热路径开销。

## 1.6.9 - 2026-08-20

### 新增

- 应用管理筛选增加当前系统/用户应用范围内的全选、取消全选和反选，并使用适配外屏高度的紧凑面板。

### 修复

- 桌面显示应用名称时为文字预留空间，图标与名称不再重叠。
- 交流反馈入口改用可识别的腾讯 QQ 企鹅图标。

## 1.6.8.13 - 2026-08-20

### 修复

- 修复竖屏展开 QS 后旋转到 270° 时磁贴偶发重叠：在旋转五列布局测量前保持单页几何，并限制安全区平移只作用于活动第一页，避免三星临时第二页进入可视区域。

## 1.6.8.12 - 2026-08-20

### 修复

- 90° / 270° 外屏 QS 五列网格按异形区安全矩形重新计算列宽、间距并居中，首列不再被页面或祖先容器裁切。
- 四个旋转方向统一交回三星原生 QS 拖动状态机，轻拉、回弹、展开和收缩进度均跟随手指，不再由 Launcher 广播强制完全展开。
- 音乐与亮度保持不透明并随 QS 页面同步滑入、滑出；单独限制纵向可见区域，缓慢下拉时不再提前完整显示或突然闪现。
- 横屏展开 QS 后转回竖屏时重新计算状态栏、媒体和亮度几何，避免沿用横屏位置。
- 外屏锁屏与 Keyguard 使用约 10 秒息屏策略；解锁桌面继续遵循系统屏幕超时，不再被锁屏时长覆盖。

### 优化

- 删除旧的非 0° Launcher 边缘手势、指针接管和 QS 强制展开广播路径，减少重复状态机与方向分支。

## 1.6.8.9 - 2026-08-18

### 修复

- 180° 下拉 QS 亮度与音乐不再重叠：转角后重抓原生位置，并按实测高度把第二块推到行距之外。
- 90° 桌面与 QS 顶部状态栏按顶边可用宽度排版，电池一侧不再顶进右侧异形区。
- 90° / 270° 下拉 QS 最左列磁贴按侧边岛区 inset，不再被裁切。
- 180° 下拉 QS 整体上移，时间/电量进顶岛，和 180° 桌面状态栏一致。
- 收起 QS 接近结束时不再提前恢复原生布局和媒体卡片，避免闪出一块框。

### 优化

- QS 下拉过程不再每帧重排亮度/音乐，打开后的滑动也不再被预绘制走树拖慢。

## 1.6.8.7 - 2026-08-18

### 修复

- 下拉 QS「亮度在上」时，亮度与音乐磁贴的间距改为实测磁贴行距加上格子内图标留白，不再比磁贴到亮度的视觉空隙更窄。
- 合盖时不再把三星主桌面 `com.sec.android.app.launcher` / `LauncherActivity` 强制钉到 display 1；只有外屏 `SecondaryLauncher` 才会被强制。避免 One UI 8.5 上桌面反复启动、transition abort 和抢占。
- 外屏 display 没有有效逻辑分辨率时不再强制 TaskLaunchParams。
- One UI 8.5 不再把 `aod.Bl1` 误当成壁纸选择器来 hook，避免 SystemUI `Integer cannot be cast to Boolean` 崩溃循环。
- 应用管理缩放改回 `density = native × percent / 100`，70% 不再把 App 放大到异常尺寸。

### 优化

- `TaskLaunchParams forced display` 热路径不再每次打 Xposed 日志，改为最多 2 秒一条。

## 1.6.8 - 2026-08-17

### 修复

- 外屏打开系统设置子页（例如开发者选项里的无线调试）不再被 CoverLauncher 延迟到内屏，也不会因为合盖伪 DeX 把外屏当成副屏而秒关。
- 系统设置、设置搜索和 SystemUI 配对界面默认允许留在外屏；从这些界面再打开的子页面同样留在外屏，避免后续其他设置页重复踩坑。

## 1.6.7 - 2026-08-15

### 修复

- 外屏 QS 磁贴编辑页添加候选或删除已有磁贴时，不再走主面板 `onMeasure` 的 `removeAllViews`/`addTile`/`tile.click()`，避免拉起设置导致 ANR、SystemUI 崩溃和 LSPosed 安全模式。
- 添加/删除/拖动排序改为下一帧再调用 `changeTilesByUser`，避免叠在 `setTiles` 布局过程中。
- 编辑页不再被三星写死的 4 列分页拆成两页：多出来的磁贴不再叠到第一行，19 个磁贴按配置网格排在单页最后一行。

## 1.6.6 - 2026-08-15

### 修复

- 外屏 QS 与通知页改为真正的左右分页：QS 内容随通知页一起移出/移入，不再只把通知页盖在静止的 QS 上面。
- 分页时保留原生窗口高斯模糊：模糊打在 `qsPanel` 上不随页面平移，避免通知页丢失毛玻璃。
- 收起 QS/通知页后恢复顶部状态栏：收起路径清掉 `qsPanel.translationX`，避免 header 被带出屏幕。
- 下拉 QS 过程中不再改写子 view 的 `translationX`，避免覆盖三星媒体面板定位，音乐面板与亮度条不再重叠。
- 通知页标题「通知」与右侧数量徽章使用同一套深底浅字颜色，深色毛玻璃上不再显示黑色标题。

### 优化

- 删除已废弃的自制外屏桌面（`CoverHomeActivity` 及其目录、图标网格、独立允许列表 Activity）与历史 `:app` 模块 Hook 源码，运行时行为不变。
- 为 `CoverSession.reduce` 会话状态机补充表驱动单元测试。

## 1.6.4 - 2026-08-14

### 修复

- 外屏 QS/通知页收起时不再先跳回 QS 页顶部或首页，收起过程保持当前画面直到面板完全离开。
- 通知页收起时保持与 QS 一致的窗口高斯模糊，不再先变透明；收起结束后也不再闪现残留的高斯背景层。
- 通知页使用返回键收起与手势上滑收起行为一致，不再先切回 QS 页。
- QS 下滚后横滑到通知页再返回，保留原滚动位置；原生布局只在面板完全收起后（不可见时机）恢复。

### 优化

- 删除已废弃的自制外屏桌面（`CoverHomeActivity` 及其目录、图标网格、独立允许列表 Activity）与历史 `:app` 模块 Hook 源码（约 4900 行），运行时行为不变。
- 为 `CoverSession.reduce` 会话状态机补充表驱动单元测试，覆盖合盖启动、开盖停止、显示身份变化和拉起次数上限等边界。

## 1.6.2 - 2026-08-12

### 新增

- 每个外屏应用支持 `70%..130%` 的全屏显示比例，以及独立的弹窗宽度、高度百分比。
- 应用管理直接嵌入控制页，支持已选应用置顶、稳定排序、搜索和筛选。

### 优化

- 侧栏采用细描边语义图标；展开时仅显示文字，收起时仅显示图标。
- 展开/收缩按钮改为安全区底部独立槽位，在不同侧栏宽度下按父容器几何中心重新居中。
- 展开或收缩侧栏时仅更新左侧导航，不再重建右侧应用管理内容。
- 显示设置按钮改为独立居中的图标按钮，深色主题弹窗统一使用主题文字和背景颜色。

### 修复

- 修复应用管理按钮选中态仍停留在上一页面、横屏侧栏缺失和底部按钮裁剪问题。
- 修复深色主题下部分按钮被系统背景 tint 二次染黑的问题。
- 配置协议升级到 v2，并兼容迁移旧版离散显示档位。

## 1.4.5 - 2026-03-14

### 新增

- 控制 App 新增“外屏列表”与独立“磁贴”一级入口，并提供浅色、深色和跟随系统的统一界面主题。
- 应用图标改为深蓝底白色几何 `F`，同步支持 adaptive icon、圆形图标和系统 monochrome 图标。
- 允许列表增加全屏搜索、系统/用户应用筛选和仅显示已允许应用筛选。

### 优化

- 重构控制 App 首页、导航、磁贴、控制和关于页面，压缩重复说明并确保六个入口与版本号完整处于外屏安全区。
- 统一所有按钮的填充、圆角和 1dp 边框，禁用系统 Z 轴动画与阴影，消除边框周围的不规则阴影。
- 更新检查仅展示严格高于当前版本的稳定 Release，并将检查中、已是最新版、新版本和失败状态改为独立状态提示。
- 允许列表改为完整覆盖物理显示的滚动内容，取消与保存按钮独立悬浮，不再显示两个按钮共用的背景容器。
- 四个旋转角度统一使用完整双按钮布局，安全 inset 避让缩减为 50%；键盘出现时自动隐藏悬浮操作。

### 修复

- 息屏、亮屏并解锁后稳定恢复原 App task/activity，避免创建新实例或错误恢复后台 App。
- 为复用的外屏桌面根窗口增加首帧门，在 decoration 回调缺失时使用 650ms fallback，消除解锁期间的可见空档。
- 动画与恢复规则严格限定到 display 1、本轮解锁代际和准确匹配的 task/root，不扩大影响 Launcher 或其他显示。

## 1.4.3 - 2026-03-14

### 修复

- 息屏、亮屏并解锁后继续恢复外屏原有 App task/activity，不创建新的 App 实例或迁移 task。
- 恢复 App task 时禁止 `TO_FRONT` 位移动画，并将同一次解锁 transition 中准确匹配的 SystemUI 锁屏 Home root 从 `TO_BACK` 规范为桌面路径一致的 `CLOSE`，使 App 与桌面都在原位置完成锁屏退出动画。
- 动画修正规则仅作用于 display 1、本轮解锁 token、记录的 App task 与对应锁屏 root，避免影响其他 display、Launcher 和无关 transition。

## 1.4.2 - 2026-03-14

### 新增

- 更新检查同时展示 GitHub Release 的版本、发布时间和 `body` 更新日志；远端内容按纯文本和长度上限处理。
- App 使用自有 adaptive icon、圆形图标和主题单色层，统一 Samsung Launcher 不同蒙版下的显示。

### 修复

- 在 `system_server` 的 Activity 启动许可边界拦截锁屏状态下 display 1 的普通外部 App 启动。
- 新增 `UNLOCKED / LOCK_PENDING / LOCKED` 状态边界；电源键锁屏提交后立即唤醒时，display 1 只恢复 Keyguard，不再恢复 SecondaryLauncher。
- 未解锁时不再允许外屏 App 创建或恢复 task，也不会通过该路径唤醒后绕过 Keyguard。
- 保留锁屏页面、系统 Home、SystemUI 和独立息屏策略的正常运行。
- App 版本号仅保留在控制页侧栏底部，不在主页或关于页重复展示。

## 1.4.1 - 2026-03-14

### 新增

- 重构模块控制页为控制中心、外屏设置、外观主题和关于四个明确页面。
- 主页增加当前版本、允许启动 App 数量和快捷设置布局概览。
- 允许列表增加“仅显示已允许应用”筛选。
- 关于页增加 GitHub Releases 更新检查、下载入口和可读错误反馈。

### 优化

- 模块页面采用 edge-to-edge 布局，背景覆盖完整外屏和左下异形区域。
- 侧栏与正文分别处理系统栏、display cutout 和强制手势 Insets，保持系统手势可用。
- 调整浅色、深色主题的色彩层级、对比度和系统栏图标外观。
- Activity 重建后保持当前页面，允许列表使用真实 IME Insets 切换紧凑布局。

### 修复

- 修复主页根容器整体避让 cutout、导致左下异形区域未被页面覆盖的问题。
- 修复更新检查失败后按钮状态和异常信息不清晰的问题。
- 所有更新与下载入口固定指向 `https://github.com/AndyNull/flexunlock/releases`。

## 1.4.0 - 2026-03-14

### 新增

- QS 根据 display 1 的真实 Configuration 和物理方向同步旋转。
- App 首帧方向策略支持 `UNSPECIFIED`、`FULL_SENSOR` 和其他传感器方向请求。
- 外屏解锁态超时读取系统 `SCREEN_OFF_TIMEOUT`，锁屏页采用独立 10 秒超时。

### 修复

- 修复 0° 下拉 QS 闪烁、App/Good Lock 异常窗口与启动动画。
- 修复 Recents snapshot、thumbnail、live surface 四方向和动态旋转。
- 修复超时后再次唤醒绕过 Keyguard、直接返回外屏 Launcher 的问题。

### 其他

- 抽屉固定为 `6 × 4`，每页最多 24 项。
- 外屏底边 RECENTS / HOME / BACK 三分手势随实时逻辑底边旋转。
- 项目和 App 版本收敛为根目录单一版本源。
