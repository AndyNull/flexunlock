# FlexUnlock Cover DeX 更新日志

## 1.7.1 - 2026-08-24

### 修复

- 完整 DeX 最近任务的“仅标题”模式恢复为从上到下的纵向列表，不再横向移出屏幕。
- 移除 Honeyboard 键盘尺寸强制 Hook，恢复系统原生键盘大小调整和二次调整能力。
- 完整 DeX 下无论是否带原始 Cover intent，普通应用均通过 system_server Hook 放行并固定到外屏。
- 普通外屏桌面不再受完整 DeX 最近任务和键盘 Hook 影响。
- 修复普通应用抽屉完成打开后因父类 `onLayout` 和重复 padding 写入而跳到左上角的问题。
- 修复底部中间 HOME 手势被误判为最近任务，恢复从抽屉返回普通桌面的行为。

### 验证

- Release 单元测试、Kotlin 编译、lint 和签名安装通过。
- Z Flip5 重启后确认 system_server Hook 加载；相册在 display 1 启动日志为 `allow=true`，未出现“需要展开手机”。

## 1.7.0 - 2026-08-23

### 新增

- 外屏控制支持读取、修改和恢复 display 1 分辨率与 DPI。
- 增加完整外屏 DeX 模式，接入三星任务栏、应用菜单和最近任务路径。
- 应用管理增加全局显示比例，并保留单应用配置优先级。

### 修复

- 完整 DeX 切换重启改由 system_server 完成，避免控制页销毁后 ENABLE 丢失、外屏停留在 SystemUI SubHome。
- 修复 One UI 8.5 锁屏访问状态、外屏 Home 所有权和应用启动 display 路由。
- 修复非 340 DPI 下 QS 亮度区域、应用密度换算和桌面图标布局异常。

### 验证

- Release 单元测试、lint 和 APK 组装通过。
- Z Flip5 覆盖安装后确认 `versionName=1.7.1`、`versionCode=10701`。

## 1.6.9.1 - 2026-08-20

### 新增

- 应用显示设置增加全局默认入口，单应用配置继续保持更高优先级。

### 修复

- 优化应用筛选弹窗分组、反选状态、图标安全尺寸和壁纸预览裁切。
- 统一 QS、通知页模糊宿主和顶部状态栏高度，减少初始下拉阶段重复写入。

## 1.6.9 - 2026-08-20

### 新增

- 应用管理筛选增加全选、取消全选和反选，并适配外屏紧凑布局。

### 修复

- 桌面显示应用名称时为文字预留空间，避免图标与名称重叠。
- 交流反馈入口改用可识别的 QQ 图标。

## 1.6.8 - 2026-08-17

### 修复

- 外屏打开系统设置子页（例如开发者选项里的无线调试）不再被 CoverLauncher 延迟到内屏，也不会因为合盖伪 DeX 把外屏当成副屏而秒关。
- 系统设置、设置搜索和 SystemUI 配对界面默认允许留在外屏；从这些界面再打开的子页面同样留在外屏，避免后续其他设置页重复踩坑。

### 验证

- Galaxy Z Flip5 外屏已验证：唤醒并滑动解锁后，「无线调试」和「显示」设置均留在外屏，不再秒关或提示去内屏。

## 1.6.7 - 2026-08-15

### 修复

- 外屏 QS 磁贴编辑页添加候选或删除已有磁贴时，不再走主面板测量过程中的重建/点击路径，避免拉起设置导致 ANR、SystemUI 崩溃和 LSPosed 安全模式。
- 添加、删除、拖动排序改为下一帧再提交系统磁贴变更，避免叠在布局过程中。
- 编辑页不再被三星写死的 4 列分页拆成两页：多出来的磁贴不再叠到第一行，按配置网格排在单页最后一行。

## 1.6.6 - 2026-08-15

### 修复

- 外屏 QS 与通知页改为真正的左右分页：QS 内容随通知页一起移出/移入，不再只把通知页盖在静止的 QS 上面。
- 分页时保留原生窗口高斯模糊：模糊打在 QS 面板上不随页面平移，避免通知页丢失毛玻璃。
- 收起 QS/通知页后恢复顶部状态栏，避免标题栏被带出屏幕。
- 下拉 QS 过程中不再改写子界面位移，避免覆盖三星媒体面板定位，音乐面板与亮度条不再重叠。
- 通知页标题「通知」与右侧数量徽章使用同一套深底浅字颜色，深色毛玻璃上不再显示黑色标题。

### 优化

- 删除已废弃的自制外屏桌面与历史 Hook，运行时仍使用三星原生外屏桌面。
- 为外屏会话状态机补充表驱动单元测试。

## 1.6.4 - 2026-08-14

### 修复

- 外屏 QS/通知页收起时不再先跳回 QS 页顶部或首页，收起过程保持当前画面直到面板完全离开。
- 通知页收起时保持与 QS 一致的窗口高斯模糊，不再先变透明；收起结束后也不再闪现残留的高斯背景层。
- 通知页使用返回键收起与手势上滑收起行为一致，不再先切回 QS 页。
- QS 下滚后横滑到通知页再返回，保留原滚动位置；原生布局只在面板完全收起后恢复。

## 1.6.2 - 2026-08-12

### 新增

- 每个外屏应用支持 `70%..130%` 的全屏显示比例，以及独立的弹窗宽度、高度百分比。
- 应用管理直接嵌入控制页，支持已选应用置顶、稳定排序、搜索和筛选。

### 优化

- 侧栏采用细描边语义图标；展开时仅显示文字，收起时仅显示图标。
- 展开/收缩按钮改为安全区底部独立槽位，在不同侧栏宽度下按父容器几何中心重新居中。
- 展开或收缩侧栏时仅更新左侧导航，不再重建右侧应用管理内容。
- 显示设置按钮改为独立居中的图标按钮，深色主题弹窗统一使用主题文字和背景颜色。

### 修复

- 修复应用管理按钮选中态仍停留在上一页面、横屏侧栏缺失和底部按钮裁剪问题。
- 修复深色主题下部分按钮被系统背景二次染黑的问题。
- 配置协议升级到 v2，并兼容迁移旧版离散显示档位。

## 1.4.5 - 2026-03-14

### 外屏锁屏与动画

- 息屏、亮屏并解锁后恢复原有 App task 和 ActivityRecord，不创建新的 App 实例，也不迁移 task。
- App 恢复时仅规范化本轮解锁准确匹配的 transition target，写入 `SHOW_WALLPAPER | NO_ANIMATION`，不扩大到其他 display 或后台 task。
- 对真正的外屏桌面复用根增加首帧门：优先等待 decoration 回调；回调缺失时使用约 650ms fallback，避免安全层释放后出现短暂空白。
- 保留 1.2s watchdog 和 fail-closed 边界，异常时不会无限阻塞锁屏退出。
- 快速息屏再亮屏只创建一个恢复代际，避免重复武装、重复恢复和错误 timeout。
- App 页面与真正桌面的标准恢复、快速恢复均已在 Galaxy Z Flip5 外屏实机验证。

### 控制 App

- 重构控制中心主页，只保留状态概览和高频启动操作。
- 左侧导航新增“外屏列表”一级入口，“磁贴”与“控制”拆分为独立页面。
- 关于页集中展示模块简介、版本、更新检查和项目入口。
- 导航压缩为适合 `748×720` 外屏的布局，六个入口和版本号均处于安全区。
- 支持浅色、深色和跟随系统主题，仅影响模块自身界面。

### 按钮与状态样式

- 统一控制 App 所有按钮的填充、圆角、文字层级和 1dp 边框。
- 禁用系统 `stateListAnimator`、`elevation` 和 `translationZ`，消除自定义边框周围的不规则阴影。
- 检查更新状态改为独立卡片，区分检查中、已是最新版、发现新版本和请求失败。
- 更新检查遍历稳定 Releases，忽略 draft、prerelease 和不高于当前版本的条目。
- 当前无更高版本时仅显示“当前已是最新版本”，不再渲染旧版本名称、日期或更新日志。

### 外屏启动允许列表

- 列表和背景覆盖完整物理显示及底部异形区域，不再由固定底栏截断。
- “取消全部”和“保存”改为覆盖在列表上方的独立悬浮按钮。
- 移除两个按钮共用的白色背景容器，按钮之间及周围直接显示列表内容。
- 四个旋转角度统一使用完整双按钮布局，不再对 0° 使用左下角异形区专用尺寸。
- 系统安全 inset 的按钮避让量缩减为 50%，在不进入异形区的前提下将按钮进一步下移。
- 列表内容可在按钮下方继续滚动，最后一项可完整滚动到按钮上方并点击。
- 搜索键盘出现时自动隐藏标题和悬浮操作，避免遮挡输入与结果列表。

### 图标与发布体验

- 应用图标改为深蓝纯色背景和白色几何 `F`。
- 同步 adaptive icon、圆形图标和 monochrome 图标，改善三星 Launcher 不同蒙版下的显示。
- App 内更新入口统一指向本仓库 Releases 页面。

### 验证

- Debug lint 与 Debug APK 构建通过。
- Release lint 与 Release APK 构建通过。
- 控制 App、更新状态和允许列表已在 `748×720 / 340dpi` 外屏验证。
- 锁屏恢复路径已覆盖普通 App、真正桌面、快速息屏亮屏以及 decoration 回调缺失场景。
